{
  "name": "@adbiljett/common",
  "version": "0.0.2",
  "description": "",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "./build/**/*"
  ],
  "scripts": {
    "clean": "rimraf ./build",
    "build": "npm run clean && tsc",
    "pub": "git add -A && git commit -m \"Update\" && git push && npm version patch && npm run build && npm publish"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.6.2"
  }
}
